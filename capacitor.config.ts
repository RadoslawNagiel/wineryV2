import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.rn.winery',
  appName: 'Winery',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
